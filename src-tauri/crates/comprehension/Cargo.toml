[package]
name = "rhema-comprehension"
version = "0.1.0"
edition = "2021"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tokio = { version = "1", default-features = false, features = ["rt", "macros"] }
